# Starter App

Minimal boilerplate generated with `create-next-app` using Next.js, TypeScript, Tailwind CSS, App Router, ESLint, Turbopack, and pnpm.

## Setup

```bash
pnpm install
pnpm dev
```

## Checks

```bash
pnpm lint
pnpm build
```
